import React from 'react'
import { Card, CardContent, Typography } from '@mui/material'

const MediaCalender = () => {
  return (
    <Card sx={{ borderRadius: '25px', height: '100%' }}>
      <CardContent>
        <Typography>MediaCalender</Typography>
      </CardContent>
    </Card>
  )
}

export default MediaCalender